// /new/about renders the classic /about page content. StaticPageLayout
// is /new-aware via usePrefixedPath so the back link and cross-nav stay in
// the new tree.
export { default } from '@/app/about/page';
export { metadata } from '@/app/about/page';
