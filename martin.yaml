# Martin tile server config — serves vector tiles from PostGIS
# auto_publish discovers all tables with geometry columns automatically.
# Martin v0.15.0 does not support auto_publish + explicit tables together.
postgres:
  connection_string: postgresql://postgres:postgres@host.docker.internal:5432/wharescore
  auto_publish:
    tables: true
